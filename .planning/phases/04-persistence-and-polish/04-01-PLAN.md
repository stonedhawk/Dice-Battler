# 04-01 Plan

## Goal

Add the MVP persistence layer and first-run guidance without expanding scope.

## Scope

- Add a `SaveService` around `shared_preferences` for `best_streak` and `tutorial_seen`.
- Surface the saved best streak on the home screen and results screens.
- Show a one-time tutorial dialog on the first run and persist the seen flag.
- Add tests for persistence behavior plus light widget checks for the saved UI state.

## Verification

- `cd app && flutter analyze`
- `cd app && flutter test`
- Confirm best streak survives restart and the tutorial dialog does not repeat after being seen once.
