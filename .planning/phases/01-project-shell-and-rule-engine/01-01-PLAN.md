---
phase: 01-project-shell-and-rule-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - app/pubspec.yaml
  - app/lib/main.dart
  - app/lib/app/app.dart
  - app/lib/app/routes.dart
  - app/lib/app/theme.dart
  - app/lib/features/home/home_screen.dart
  - app/lib/features/battle/battle_screen.dart
  - app/lib/features/battle/battle_models.dart
  - app/lib/features/battle/enemy_factory.dart
  - app/lib/features/battle/turn_resolver.dart
  - app/lib/features/rewards/reward_models.dart
  - app/lib/features/run/run_state.dart
  - app/test/unit/battle/turn_resolver_test.dart
  - app/test/unit/battle/enemy_factory_test.dart
  - app/test/widget/home_screen_test.dart
autonomous: true
requirements:
  - APP-01
  - APP-02
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
  - TEST-01
user_setup:
  - Make the Flutter SDK available on PATH so `flutter analyze` and `flutter test` can run
must_haves:
  truths:
    - The repo contains a lean `.planning/` project aligned to the existing project docs
    - The `/app` directory contains a beginner-friendly Flutter shell with a home screen and route wiring
    - Core battle math is implemented in pure Dart and covered by tests
  artifacts:
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - app/lib/features/battle/turn_resolver.dart
    - app/lib/features/battle/enemy_factory.dart
    - app/test/unit/battle/turn_resolver_test.dart
    - app/test/unit/battle/enemy_factory_test.dart
  key_links:
    - The battle models are shared by the UI shell, turn resolver, and tests
    - The roadmap and requirements map Milestone 1 to Phase 1
---

<objective>
Bootstrap the repo into a lean GSD project and implement Milestone 1 without adding scope beyond the docs.

Purpose: Set up the planning backbone and the first working slice of code so later phases can focus on gameplay UI and run flow.
Output: `.planning/` docs, the Phase 1 `/app` shell, pure battle logic, enemy factory, and starter tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@PRD.md
@TECHNICAL_DESIGN.md
@IMPLEMENTATION_PLAN.md
@QA_CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap lean GSD planning files</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/config.json</files>
  <read_first>PRD.md, TECHNICAL_DESIGN.md, IMPLEMENTATION_PLAN.md, QA_CHECKLIST.md</read_first>
  <action>Create a lightweight `.planning/` scaffold that mirrors the existing product docs instead of inventing new scope. Keep the roadmap at five phases matching the milestone plan and mark Phase 1 as the active focus.</action>
  <verify>test -f .planning/PROJECT.md && test -f .planning/ROADMAP.md</verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `Phase 1: Project shell and rule engine`
    - `.planning/STATE.md` contains `Current focus: Phase 1: Project shell and rule engine`
  </acceptance_criteria>
  <done>Lean GSD files exist and align to the current docs.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 1 Flutter shell and domain models</name>
  <files>app/pubspec.yaml, app/lib/main.dart, app/lib/app/app.dart, app/lib/app/routes.dart, app/lib/app/theme.dart, app/lib/features/home/home_screen.dart, app/lib/features/battle/battle_screen.dart, app/lib/features/battle/battle_models.dart, app/lib/features/rewards/reward_models.dart, app/lib/features/run/run_state.dart</files>
  <read_first>TECHNICAL_DESIGN.md, IMPLEMENTATION_PLAN.md</read_first>
  <action>Create a minimal beginner-friendly Flutter structure under `/app` with a home screen, Start Run entry point, route wiring, placeholder battle screen, and immutable models for dice, player, enemy, rewards, and run state. Keep gameplay UI intentionally thin because Milestone 2 owns the battle experience.</action>
  <verify>test -f app/lib/features/home/home_screen.dart && test -f app/lib/features/battle/battle_models.dart</verify>
  <acceptance_criteria>
    - `app/lib/features/home/home_screen.dart` contains `Start Run`
    - `app/lib/features/battle/battle_models.dart` contains `enum ActionType`
  </acceptance_criteria>
  <done>Phase 1 shell files and models exist under `/app`.</done>
</task>

<task type="auto">
  <name>Task 3: Implement deterministic battle logic and Phase 1 tests</name>
  <files>app/lib/features/battle/turn_resolver.dart, app/lib/features/battle/enemy_factory.dart, app/test/unit/battle/turn_resolver_test.dart, app/test/unit/battle/enemy_factory_test.dart, app/test/widget/home_screen_test.dart</files>
  <read_first>PRD.md, QA_CHECKLIST.md</read_first>
  <action>Implement the rule math as pure Dart with no UI dependencies. Cover mixed assignments, shield absorption, heal caps, enemy defeat before retaliation, and battle scaling in tests. Add a simple home-screen smoke test as a shell check.</action>
  <verify>test -f app/lib/features/battle/turn_resolver.dart && test -f app/test/unit/battle/turn_resolver_test.dart</verify>
  <acceptance_criteria>
    - `app/lib/features/battle/turn_resolver.dart` contains `TurnOutcome resolveTurn`
    - `app/lib/features/battle/enemy_factory.dart` contains `createEnemyForBattle`
    - `app/test/unit/battle/turn_resolver_test.dart` contains `caps healing at the player's max HP`
  </acceptance_criteria>
  <done>Pure rule logic and starter tests are present for Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && flutter analyze`
- [ ] `cd app && flutter test`
- [ ] Confirm the Phase 1 deliverables and QA expectations are satisfied
</verification>

<success_criteria>

- All Phase 1 files are created without adding out-of-scope systems
- The repo is ready for future GSD phases
- The remaining blocker is local Flutter toolchain availability, not missing Phase 1 source files
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-shell-and-rule-engine/01-01-SUMMARY.md`
</output>
