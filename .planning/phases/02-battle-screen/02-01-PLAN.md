# 02-01 Plan

## Goal

Turn the pure rule engine into one full playable battle loop.

## Scope

- Build a `BattleController` that owns dice assignment, turn resolution, and restart state.
- Replace the placeholder battle screen with a real UI for enemy intent, dice assignment, and resolve flow.
- Add widget coverage for disabled resolve state plus win/restart behavior.

## Verification

- `cd app && flutter analyze`
- `cd app && flutter test`
- Confirm one battle can be won or lost from the UI and restarted cleanly.
